[package]
name = "openvx-test"
version = "0.0.1"
publish = false

[dependencies]
vx = { path = "./vx" }

[workspace]
members = [ "vx" ]

[dev-dependencies]
cv = { git = "https://github.com/nebgnahz/cv-rs.git" }
